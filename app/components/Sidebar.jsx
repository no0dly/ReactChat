var React = require('react');

var Sidebar = React.createClass({
    render() {
        return (
            <div className="sidebar">
                Sidebar component
            </div>
        );
    }
});

export default Sidebar;
